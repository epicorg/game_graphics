package game.net.fieldsnames;

/**
 * Possible common fields.
 */

public enum CommonFields {
	
     IP_ADDRESS,
     RESULT,
     HASHCODE,
     LIST,
     USERNAME,
     PASSWORD,
     LOCAL_PORT,
     NO_ERRORS,
     ERRORS,
     INVALID,
     OFFLINE,
     SERVER_ERROR,
     MISSING_FIELD
}
